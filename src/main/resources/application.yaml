server:
  port: 8080

spring:
  datasource:
    username: root
    password: rootroot
    url: jdbc:mysql://localhost:3306/search_engine?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        connection:
          isolation: 8
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

indexing-settings:
  sites:
    - url: https://lenta.ru
      name: Lenta
    - url: https://beltexoptic.by
      name: Белтекс Оптик
    - url: https://bizlida.by
      name: BizLida
    - url: https://www.onliner.by/
      name: Onliner
    - url: https://atlant.by/
      name: Atlant